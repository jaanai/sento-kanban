// Page content
$('#board-content').html('<%= j render(partial: "board_columns") %>');

applyDragula();

<% if @open_card && @ask_username == false %>
  $('.sento-kanban-cards a.card-action-link[data-card-id="<%= @open_card %>"]').trigger('click');
<% end %>
